{
  "name": "bun-biome-boilerplate",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "start": "bun run .",
    "format": "bunx @biomejs/biome format --write ./src",
    "format:check": "bunx @biomejs/biome format ./src",
    "lint": "bunx @biomejs/biome check --apply ./src",
    "lint:check": "bunx @biomejs/biome check ./src"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.6.4",
    "@types/bun": "latest",
    "lefthook": "^1.6.10"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
